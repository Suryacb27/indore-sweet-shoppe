# Technology Stack

> Auto-generated by /map on 2026-02-24

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| Node.js | ≥18 (implied by Next 16) | Server runtime |
| TypeScript | ^5 | Type-safe development language |
| Next.js | 16.1.6 | Full-stack React framework (App Router) |
| React | 19.2.3 | UI library |
| React DOM | 19.2.3 | DOM renderer |

---

## Production Dependencies

| Package | Version | Purpose |
|---------|---------|---------|
| `@supabase/supabase-js` | ^2.97.0 | Supabase JS client — DB queries, auth |
| `@supabase/ssr` | ^0.8.0 | Supabase SSR helpers — cookie-based session for Next.js server/middleware |
| `lucide-react` | ^0.575.0 | Icon library used throughout UI |
| `clsx` | ^2.1.1 | Conditional class name utility |
| `tailwind-merge` | ^3.5.0 | Merge Tailwind classes safely (avoids conflicts) |

---

## Development Dependencies

| Package | Version | Purpose |
|---------|---------|---------|
| `tailwindcss` | ^4 | Utility-first CSS framework |
| `@tailwindcss/postcss` | ^4 | PostCSS plugin for Tailwind v4 |
| `typescript` | ^5 | TypeScript compiler |
| `eslint` | ^9 | JavaScript/TypeScript linter |
| `eslint-config-next` | 16.1.6 | ESLint rules for Next.js |
| `@types/node` | ^20 | TypeScript types for Node.js |
| `@types/react` | ^19 | TypeScript types for React |
| `@types/react-dom` | ^19 | TypeScript types for React DOM |

---

## Infrastructure & Services

| Service | Provider | Purpose |
|---------|----------|---------|
| Authentication | Supabase Auth | Email/password auth, JWT sessions via cookies |
| Database | Supabase PostgreSQL | Products, categories, orders, cart, profiles |
| Stored Procedures | Supabase RPC | `create_order_transaction` — atomic order creation |
| Fonts | Google Fonts CDN | Plus Jakarta Sans, Playfair Display, Manrope, Material Symbols |
| Hosting | (Not configured) | No deployment config present |

---

## Configuration

| Variable | Purpose | Required | Location |
|----------|---------|----------|----------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL | Yes | `.env.local` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anonymous JWT (safe for browser) | Yes | `.env.local` |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service role JWT (server-only) | Yes | `.env.local` |

---

## Typography

| Font | Variable | Usage |
|------|----------|-------|
| Plus Jakarta Sans | `--font-sans` | Body text, default |
| Playfair Display | `--font-serif` | Headings, admin dashboard serif italic |
| Manrope | `--font-manrope` | Alternative sans |
| Material Symbols Outlined | (Google CDN) | Icon font used in Navbar |

---

## Architecture Patterns

| Pattern | Implementation |
|---------|---------------|
| Server Components | Default for all route pages — data fetching at render time |
| Server Actions | `"use server"` files in `actions/` — form mutations |
| Client Components | `"use client"` only for interactive UI (forms, mobile menu) |
| Cookie-based Sessions | `@supabase/ssr` handles cookie read/write in middleware & server |
| Role-Based Access | Middleware + layout double-guard on `/admin/*` routes |
| Atomic Transactions | Supabase RPC for order creation |

---

## No Payment Gateway

> The `orders.payment_status` field exists but no payment processor (Razorpay, Stripe, etc.) is integrated. Orders are assumed to be Cash on Delivery or pending manual payment handling.

---

## No Test Framework

> There is no test framework configured (`jest`, `vitest`, `playwright`, etc.). The project has zero test files.
